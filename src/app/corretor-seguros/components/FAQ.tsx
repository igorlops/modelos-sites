'use client'; import { theme } from '../theme'; import { useState } from 'react'; import { ChevronDown } from 'lucide-react'; export default function FAQ() { const [open, setOpen] = useState<number | null>(0); const faqs = [{ q: "Pago algo para o corretor?", a: "Não. Nosso trabalho é remunerado pela seguradora via comissão. Para você, o valor é o mesmo." }, { q: "Posso trocar de seguradora depois?", a: "Sim, anualmente ou quando sua apólice vencer. Faremos novas cotações sempre que solicitar." }, { q: "Quanto tempo demora para aprovar?", a: "Seguro auto: 24-48h. Vida: até 7 dias (se houver exame médico). Residencial: aprovação imediata." }, { q: "Como funciona em caso de sinistro?", a: "Você nos aciona, nós acionamos a seguradora e acompanhamos todo o processo até a indenização." }]; return (<section className="py-24 bg-white"><div className={theme.layout.container}><h2 className="text-4xl font-bold text-center text-slate-900 mb-16">Dúvidas Comuns</h2><div className="max-w-3xl mx-auto space-y-4">{faqs.map((f, i) => (<div key={i} className="border border-slate-200 rounded-xl overflow-hidden"><button onClick={() => setOpen(open === i ? null : i)} className="w-full flex justify-between p-6 text-left hover:bg-slate-50"><span className={`font-bold ${open === i ? 'text-indigo-700' : 'text-slate-900'}`}>{f.q}</span><ChevronDown className={`text-slate-400 transition-transform ${open === i ? 'rotate-180' : ''}`} /></button><div className={`px-6 transition-all ${open === i ? 'max-h-24 pb-6' : 'max-h-0 opacity-0'}`}><p className="text-slate-600">{f.a}</p></div></div>))}</div></div></section>); }
