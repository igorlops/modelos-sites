'use client'; import { theme } from '../theme'; import { useState } from 'react'; import { Plus } from 'lucide-react'; export default function FAQ() { const [o, setO] = useState<number | null>(0); const faqs = [{ q: "Tem limite de leads/contatos?", a: "Não. Armazene quantos leads quiser. Cobramos apenas por usuário ativo (vendedor/manager que loga)." }, { q: "Posso personalizar as etapas do funil?", a: "Sim, totalmente. Crie quantas etapas quiser, nomeie do seu jeito, adicione campos customizados." }, { q: "Integra com meu sistema atual?", a: "API aberta REST + webhooks. Se seu sistema tem API, integramos. Temos conectores prontos para 50+ ferramentas." }, { q: "Tem app offline?", a: "Sim. Dados sincronizam quando voltar internet. Vendedores em áreas sem sinal não param de trabalhar." }, { q: "Como funciona a migração?", a: "Agendamos chamada, você envia CSV/Excel, importamos e mapeamos campos. Setup completo em 1 dia." }]; return (<section className="py-24 bg-white"><div className={theme.layout.container}><h2 className="text-4xl font-bold text-center mb-16">Dúvidas Comuns</h2><div className="max-w-3xl mx-auto space-y-4">{faqs.map((f, i) => (<div key={i} className="border border-slate-200 rounded-xl overflow-hidden"><button onClick={() => setO(o === i ? null : i)} className="w-full flex justify-between p-6 text-left hover:bg-slate-50"><span className={`font-bold ${o === i ? 'text-blue-600' : 'text-slate-900'}`}>{f.q}</span><Plus className={`transition-transform ${o === i ? 'rotate-45' : ''}`} /></button><div className={`px-6 transition-all ${o === i ? 'max-h-28 pb-6' : 'max-h-0 opacity-0'}`}><p className="text-slate-600">{f.a}</p></div></div>))}</div></div></section>); }
